"use client";

import PagePlaceholder from "@/components/dashboard/PagePlaceholder";

export default function SettingsPage() {
  return <PagePlaceholder title="Pengaturan Sistem & Role" description="Configure system settings, roles, and permissions" />;
}